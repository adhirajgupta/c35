var canvas,backgroundimg;
var gamestate = 0;
var playerCount;
var database;
var form,game,player

function setup(){
  canvas = createCanvas(400,400);
  database = firebase.database;
}
function draw(){
  
}